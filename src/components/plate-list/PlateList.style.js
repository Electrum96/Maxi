import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 360,
     
  },
  wrapper: {
    flexWrap: 'wrap',
    gap: 20,
    paddingRight: 30
  }
});

export default styles;
